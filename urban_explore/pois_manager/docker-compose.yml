services:
  poi-viewer:
    build: .
    container_name: poi-viewer
    ports:
      - "8080:8080"
    volumes:
      - ./static:/app/static
      - ./data:/app/data
      - ./templates:/app/templates
    restart: unless-stopped
