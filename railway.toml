[build]
builder = "nixpacks"

[deploy]
startCommand = "cd urban-explore/pois-manager && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30

[variables]
# Variables se configuran en la web interface de Railway